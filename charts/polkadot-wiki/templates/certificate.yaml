{{ if ne .Values.environment "ci" }}
apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: {{ .Release.Name }}
  namespace: default
spec:
  secretName: {{ .Release.Name }}-tls
  issuerRef:
    name: letsencrypt
  dnsNames:
  - {{ .Values.domain }}
  acme:
    config:
    - dns01:
        provider: prod-dns
      domains:
      - {{ .Values.domain }}
{{ end }}
